# 工具清单（制度/知识调用）

需要查询制度、流程、规定类内容时使用通知公告工具。

- `searchNotice`：按关键词搜索通知/公告/制度，参数 keyword(字符串，至少 1 字符)
- `getLatestNotices`：最新 N 条通知，参数 limit(整数，默认 5)
- `getNoticeById`：按 ID 查通知详情，参数 id(整数)

如果 Notice 搜索结果与问题相关性弱，则告知："系统中暂未收录该类制度文档，建议向教务确认最新流程。"
