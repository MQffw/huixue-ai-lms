<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.EmpMapper">

    <!-- 通用结果映射 -->
    <resultMap id="EmpResultMap" type="com.itheima.pojo.Emp">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="name" property="name" />
        <result column="gender" property="gender" />
        <result column="phone" property="phone" />
        <result column="id_card" property="idCard" />
        <result column="job" property="job" />
        <result column="salary" property="salary" />
        <result column="image" property="image" />
        <result column="entry_date" property="entryDate" />
        <result column="dept_id" property="deptId" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 根据用户名查询 -->
    <select id="findByUsername" resultMap="EmpResultMap">
        SELECT * FROM emp WHERE username = #{username}
    </select>

    <!-- 分页条件查询 -->
    <select id="pageList" resultMap="EmpResultMap">
        SELECT e.*, d.name as deptName
        FROM emp e
        LEFT JOIN dept d ON e.dept_id = d.id
        <where>
            <if test="name != null and name != ''">
                AND e.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="gender != null">
                AND e.gender = #{gender}
            </if>
            <if test="beginDate != null">
                AND e.entry_date >= #{beginDate}
            </if>
            <if test="endDate != null">
                AND e.entry_date &lt;= #{endDate}
            </if>
            <if test="deptId != null">
                AND e.dept_id = #{deptId}
            </if>
        </where>
        ORDER BY e.create_time DESC
        LIMIT #{start}, #{pageSize}
    </select>

    <!-- 统计总数 -->
    <select id="count" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM emp e
        LEFT JOIN dept d ON e.dept_id = d.id
        <where>
            <if test="name != null and name != ''">
                AND e.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="gender != null">
                AND e.gender = #{gender}
            </if>
            <if test="beginDate != null">
                AND e.entry_date >= #{beginDate}
            </if>
            <if test="endDate != null">
                AND e.entry_date &lt;= #{endDate}
            </if>
            <if test="deptId != null">
                AND e.dept_id = #{deptId}
            </if>
        </where>
    </select>

    <!-- 批量删除 -->
    <delete id="deleteByIds">
        DELETE FROM emp WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 新增员工 -->
    <insert id="insert" parameterType="com.itheima.pojo.Emp" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO emp (username, password, name, gender, phone, id_card, job, salary, image, entry_date, dept_id, create_time, update_time)
        VALUES (#{username}, #{password}, #{name}, #{gender}, #{phone}, #{idCard}, #{job}, #{salary}, #{image}, #{entryDate}, #{deptId}, #{createTime}, #{updateTime})
    </insert>

    <!-- 根据ID查询 -->
    <select id="getById" resultMap="EmpResultMap">
        SELECT e.*, d.name as deptName
        FROM emp e
        LEFT JOIN dept d ON e.dept_id = d.id
        WHERE e.id = #{id}
    </select>

    <!-- 更新员工 -->
    <update id="updateById" parameterType="com.itheima.pojo.Emp">
        UPDATE emp
        <set>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="idCard != null">id_card = #{idCard},</if>
            <if test="job != null">job = #{job},</if>
            <if test="salary != null">salary = #{salary},</if>
            <if test="image != null">image = #{image},</if>
            <if test="entryDate != null">entry_date = #{entryDate},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <!-- 查询所有员工 -->
    <select id="findAll" resultMap="EmpResultMap">
        SELECT e.*, d.name as deptName
        FROM emp e
        LEFT JOIN dept d ON e.dept_id = d.id
        ORDER BY e.create_time DESC
    </select>

    <!-- 统计员工职位分布 -->
    <select id="countEmpJobData" resultType="map">
        SELECT
            CASE job
                WHEN 1 THEN '班主任'
                WHEN 2 THEN '讲师'
                WHEN 3 THEN '学工主管'
                WHEN 4 THEN '教研主管'
                WHEN 5 THEN '咨询师'
                ELSE '其他'
            END as pos,
            COUNT(*) as num
        FROM emp
        WHERE job IS NOT NULL
        GROUP BY job
        ORDER BY num DESC
    </select>

    <!-- 统计员工性别分布 -->
    <select id="countEmpGenderData" resultType="map">
        SELECT 
            CASE gender
                WHEN 1 THEN '男'
                WHEN 2 THEN '女'
                ELSE '未知'
            END as pos,
            COUNT(*) as num
        FROM emp
        GROUP BY gender
    </select>

    <!-- 修改密码 -->
    <update id="updatePassword">
        UPDATE emp SET password = #{newPassword}, update_time = NOW() WHERE id = #{id}
    </update>

</mapper>
